#include <stdio.h>
int main()
{
    char c;
    short s;
    printf("size of char:%d byte(S)\n",sizeof (c));
    printf("size of short:%d byte(S)\n",sizeof (s));
    printf("size of float:%dbyte(s)\n",sizeof (float));
    return 0;
}
    
